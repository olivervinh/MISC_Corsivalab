import {createSlice} from "@reduxjs/toolkit"
const TotalConfirm2021Slice = createSlice({
    name:"TotalConfirm2021",
    initialState:{
        object:{
            data : null,
            isFetching :false,
            error:false 
        },
        msg: ""
    },
    reducers:{
        start:(state)=>{
            state.object.isFetching = true
        },
        success:(state,action)=>{
            state.object.isFetching = false
            state.object.data = action.payload
        },
        failed :(state,action)=>{
            state.object.isFetching=false
            state.object.error = true
            state.msg = action.payload
        }
    }
})
export const {
    start,
    success,
    failed
} = TotalConfirm2021Slice.actions
export default TotalConfirm2021Slice.reducer