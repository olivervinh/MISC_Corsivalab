import {createSlice} from "@reduxjs/toolkit"
const TotalForecast2020Slice = createSlice({
    name:"TotalForecast2020",
    initialState:{
        object:{
            data : null,
            isFetching :false,
            error:false 
        },
        msg: ""
    },
    reducers:{
        start:(state)=>{
            state.object.isFetching = true
        },
        success:(state,action)=>{
            state.object.isFetching = false
            state.object.data = action.payload
        },
        failed :(state,action)=>{
            state.object.isFetching=false
            state.object.error = true
            state.msg = action.payload
        }
    }
})
export const {
    start,
    success,
    failed
} = TotalForecast2020Slice.actions
export default TotalForecast2020Slice.reducer